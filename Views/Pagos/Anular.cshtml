@model Laboratorio_II___Proyecto_Inmobiliaria_EnzoMiranda.Models.Pago

@{
    ViewData["Title"] = $"Anular Pago #{Model.NumeroPago}";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm border-danger rounded-3">
            <div class="card-header bg-danger text-white py-3">
                <h4 class="mb-0 fw-bold"><i class="bi bi-exclamation-triangle-fill me-2"></i>Confirmar AnulaciÃ³n de Pago</h4>
            </div>
            <div class="card-body p-4 text-center">
                <i class="bi bi-x-circle text-danger display-3 d-block mb-3"></i>
                <h5 class="fw-bold mb-3">Â¿EstÃ¡ seguro de que desea anular este comprobante de pago?</h5>
                
                <div class="alert alert-light border text-start mb-4">
                    <p class="mb-1"><strong>NÂ° de Pago:</strong> #@Model.NumeroPago</p>
                    <p class="mb-1"><strong>Reserva:</strong> #@Model.IdReserva</p>
                    <p class="mb-1"><strong>Importe:</strong> <span class="text-danger fw-bold">$@Model.Importe.ToString("N2")</span></p>
                    <p class="mb-1"><strong>Fecha:</strong> @Model.FechaPago.ToString("dd/MM/yyyy HH:mm")</p>
                    <p class="mb-0"><strong>Concepto:</strong> @Model.Concepto</p>
                </div>

                <p class="text-muted small">
                    El registro no serÃ¡ borrado fÃ­sicamente de la base de datos; quedarÃ¡ registrado como <strong>Anulado</strong> y se guardarÃ¡ la fecha y su usuario para fines de auditorÃ­a.
                </p>

                <form asp-action="ConfirmarAnular" method="post" class="d-flex justify-content-center gap-3 mt-4">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="idPago" value="@Model.IdPago" />

                    <a asp-action="Details" asp-route-id="@Model.IdPago" class="btn btn-outline-secondary px-4">
                        Cancelar
                    </a>
                    <button type="submit" class="btn btn-danger px-4 shadow-sm">
                        <i class="bi bi-x-octagon me-1"></i> SÃ­, Anular Pago
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
